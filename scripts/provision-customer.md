# Provision a ProcureFlow customer (operator checklist)

The checklist and the commands live in **[docs/DEPLOY_MANUAL.md](../docs/DEPLOY_MANUAL.md)**. Use that manual. This file does not repeat the steps.

Preferred entry: `npm run onboard:customer` (dry-run, then `--apply`). Stepped path: `npm run turso:customer`, then the Vercel and provision commands in the manual.

Capabilities: [docs/SYSTEM_MANUAL.md](../docs/SYSTEM_MANUAL.md). Older pointers: [docs/CUSTOMER_ONBOARDING.md](../docs/CUSTOMER_ONBOARDING.md), [docs/DEPLOYMENT.md](../docs/DEPLOYMENT.md). Env names: [`.env.example`](../.env.example).
